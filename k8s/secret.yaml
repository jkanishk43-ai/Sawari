apiVersion: v1
kind: Secret
metadata:
  name: sawaari-backend-secret
  namespace: sawaari
type: Opaque
stringData:
  # Database credentials
  POSTGRES_PASSWORD: "CHANGE_IN_PRODUCTION"
  CLICKHOUSE_PASSWORD: "CHANGE_IN_PRODUCTION"

  # JWT
  JWT_SECRET: "CHANGE_IN_PRODUCTION"

  # S3/MinIO
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "CHANGE_IN_PRODUCTION"

  # External APIs
  OTD_API_KEY: "CHANGE_IN_PRODUCTION"
  MSG91_API_KEY: "CHANGE_IN_PRODUCTION"

  # ONDC
  ONDC_SUBSCRIBER_ID: "CHANGE_IN_PRODUCTION"
  ONDC_SUBSCRIBER_KEY: "CHANGE_IN_PRODUCTION"

  # Grafana
  GRAFANA_PASSWORD: "CHANGE_IN_PRODUCTION"
